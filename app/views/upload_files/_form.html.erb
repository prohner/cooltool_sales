<%= form_for @upload_file, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :filename, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :filename, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :sales_date, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :sales_date, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                upload_files_path, :class => 'btn' %>
  </div>
<% end %>
